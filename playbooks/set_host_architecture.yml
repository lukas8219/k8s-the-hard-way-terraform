---
- name: Set host architecture
  set_fact:
    host_arch: >-
      {{
        {
          'x86_64': 'amd64',
          'aarch64': 'arm64',
          'arm64': 'arm64'
        }[ansible_architecture]
      }}

